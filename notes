ArrayList<Integer> al = new ArrayList<Integer>();
    al.contains(1);
    al.get(0);
    al.remove(0);
    al.size();
    al.isEmpty();
    al.add(object);
    al.add(index,object);
    al.set(index,object);
int [] a = Arrays.copyOfRange(a, 1, 5);
